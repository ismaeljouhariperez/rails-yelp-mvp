
<%= render 'restaurant', restaurant: @restaurant %>
<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <%= link_to 'Edit', edit_restaurant_path(@restaurant), class: 'text-center btn btn-primary' %>
      <%= link_to 'See all reviews', restaurant_reviews_path(@restaurant), class: 'text-center btn btn-secondary' %>
      <%= link_to 'All restaurants', restaurants_path, class: 'text-center btn btn-secondary' %>
  </div>
</div>
